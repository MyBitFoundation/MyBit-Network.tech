{
  "name": "mybitdapp",
  "version": "1.0.0",
  "description": "The contracts are created to be upgradeable as they save all long-term data in Database.sol. As development continues and contracts change, we will use the upgrading protocol written in ContractManager.sol. For this reason the current address + abi of the contracts will be changing throughout the Alpha.",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "truffle compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "solc": "0.4.23",
    "truffle": "^5.0.0-byoc.2"
  }
}
